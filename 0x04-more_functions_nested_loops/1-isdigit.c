#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9).
 * @c: the character to be checked if it is 0 through 9..
 * Return: 1 if c is 0 through 9 or 0 if otherwise.
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
