#include "main.h"
/**
 * mul - It multiplies two integers.
 * @a: first integer
 * @b: second integer
 * Return: result (success)
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
